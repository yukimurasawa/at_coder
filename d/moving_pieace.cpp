#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
  int k, n;
  cin >> k >> n;
  vector<int> p(n);
  for (int i = 0; i < n; i++)
  {
    cin >> p[i];
  }
  vector<int> c(n);
  for (int i = 0; i < n; i++)
  {
    cin >> c[i];
  }
  return 0;
}
